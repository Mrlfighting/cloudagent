# ── 密钥与本地配置：绝不提交 ──────────────────────────────────
# 本仓库历史上出现过两次 .env 误提交事故（提交 7c4067a、2928754）。
# 提交前务必执行 git status 核对，确认暂存区里没有 .env 类文件。
.env
agent/.env
front/cloud_agent/.env.local

# ── Claude Code 本地记忆：按约定保留在本地，不随仓库分发 ──────
# CLAUDE.md 承载项目背景与开发约束，只对本机生效。
# .claude/ 下是本机权限配置（含绝对路径），同样不该进仓库。
# 若日后需要团队共享配置，可改为只忽略 .claude/settings.local.json。
CLAUDE.md
.claude/

# ── Python 编译产物 ──────────────────────────────────────────
# 注意：忽略规则对「已被跟踪」的文件无效。历史上 .pyc 就是先提交后加规则，
# 导致 git status 长期挂着 86 个 .pyc 的噪音。若再有同类情况，需先执行
# git rm --cached 把它们移出索引。
__pycache__/
*.py[cod]
*.pyo

# ── 运行时产物与日志 ─────────────────────────────────────────
run_logs/
*.log

# ── 前端构建产物与测试报告 ───────────────────────────────────
dist/
node_modules/
test-results/
playwright-report/

# ── 测试缓存 ────────────────────────────────────────────────
.pytest_cache/

# ── 操作系统杂项 ────────────────────────────────────────────
.DS_Store
